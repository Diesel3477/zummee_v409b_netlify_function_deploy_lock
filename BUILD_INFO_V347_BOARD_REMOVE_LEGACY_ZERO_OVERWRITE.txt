v347 removes the legacy v307 auth bootstrap overwrite path and makes the visible Board Member Hub submitted list render directly from BoardMemberActionItems.status=open, with a guard to preserve the last good source rows if hidden legacy iframe output tries to overwrite with a stale zero.
