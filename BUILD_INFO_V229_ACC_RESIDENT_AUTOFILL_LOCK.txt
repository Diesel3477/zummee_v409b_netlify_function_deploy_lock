V229: ACC page resident auto-fill now uses the same resident auth/session sources as working resident pages, plus a resident_directory fallback by auth_user_id, resident id, or email. Name and address/unit fields are locked after hydration.
