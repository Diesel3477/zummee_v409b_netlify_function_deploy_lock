Fixes in this build:
- assign-vendor-email now includes Accept Job twice in HTML email and as a plain-text fallback link.
- vendor email payload now includes a text body with all action links.
- vendor_accept.html and vendor_work_order.html now send Authorization header along with apikey and show fuller errors.

Important: changing the vendor email body requires deploying supabase/functions/assign-vendor-email.
Fixing the accept action requires deploying supabase/functions/accept-vendor-job.
