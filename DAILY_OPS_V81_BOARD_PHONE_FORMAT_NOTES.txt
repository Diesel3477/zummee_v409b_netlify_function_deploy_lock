Daily Ops v81 Board Member Phone Format
- Board member phone input now auto-formats to 111-111-1111 while typing/pasting.
- Saved board member phone values are normalized to 111-111-1111.
- Save is blocked if a non-empty board member phone number is not exactly 10 digits.
- Board member table display uses the same normalized format; Call/Text links use digits only.
