V133 Community Assignments show-all fix

- Current Assignments now loads by company_scope as well as company_name/company_id.
- Remote assignment rows now retain company_scope.
- Upsert uses company_scope + community_id so every community assignment stays separate.
- Fallback filtering no longer drops rows that only have company_scope populated.
