This build removes vendor_name from the maintenance_requests insert payload.

Why:
Your maintenance_requests table does not have a vendor_name column.

What changed:
- removed vendor_name from the primary maintenance_requests payload
- removed vendor_name from the fallback maintenance_requests payload

Expected result:
- vendor email still sends
- maintenance_requests POST should stop throwing the vendor_name schema error
