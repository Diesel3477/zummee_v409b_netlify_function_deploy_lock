v324: Hard Supabase session gate for Board Hub and Manager Hub board counts. Prevents false zero when iPad has no active Supabase session. Uses BoardMemberActionItems status=open only after active auth session is present.
