v55 fixes

1. Call Manager link:
- changed the button-style call link to a simpler email-safe tel: link format
- many email clients strip or neutralize heavily styled tel buttons

2. Completed status protection:
- assigning/notifying a vendor will no longer downgrade a completed work order back to active
- if the selected work order is already completed, the page preserves completed status
