v402a safe shared data layer build

Base: zummee_v401_board_v2_dropdown_full_list_fix.zip

Changes:
- Added shared/data-layer.js as a safe additive module.
- Manager Hub loads the shared layer but keeps its existing stable dropdown boot path.
- Board Hub V2 uses the shared strict assigned-community loader first.
- Board Hub V2 no longer slices submitted board items to 8; it renders all active records returned.
- No all-company community fallback was added to Board Hub dropdown.
