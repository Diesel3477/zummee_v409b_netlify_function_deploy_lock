V134 Community Assignments Show All Rows Fix

- Current Assignments now builds its display from the full company community list plus saved assignment rows.
- Remote assignment rows are merged with local assignments instead of replacing them.
- This prevents the table from collapsing to only the two rows currently returned from the cloud assignments table.
- If a community exists but has no saved assignment row yet, it still appears so the missing saved assignment is visible instead of hidden.
