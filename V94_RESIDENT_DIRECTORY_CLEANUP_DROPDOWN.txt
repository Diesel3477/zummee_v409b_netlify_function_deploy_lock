v94 resident directory cleanup + community dropdown

What changed:
- removed duplicate Resident Directory sections
- kept only the intended 2-column layout
- added community dropdown so staff no longer need to paste UUIDs
- manual entry now uses a compact form with optional extra address fields

No new function deploy is required.