Resident portal security build

Included changes:
- resident_portal.html added as the resident landing page
- login redirects Resident users to resident_portal.html after sign-in
- manager hub redirects Resident users to resident_portal.html
- resident_directory.html redirects Resident users away from manager-only page
- resident_work_orders.html redirects Resident users away from manager-only page
- work_order_submit.html auto-detects logged-in resident, auto-fills resident info, locks resident name/unit/email, and prevents community mismatch

Recommended next database hardening step:
- Add RLS policies on resident_directory and resident_work_orders so residents can only read their own rows at the database layer.
- Current build adds strong UI/session gating first so you can test the portal flow immediately.
