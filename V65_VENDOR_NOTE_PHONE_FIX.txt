v65 vendor note + resident phone fix

Changes:
- resident_work_orders.html now saves the note-to-vendor into:
  assigned_vendor_note
- vendor_work_order.html now renders the resident phone as a clickable tel: link

Why:
- the vendor work order page reads assigned_vendor_note, so the note must be saved onto the work order
- vendors may need to call the resident directly from the work order page